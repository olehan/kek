language: go

os:
  - osx
  - linux
  - windows

go:
  - 1.11.x
  - 1.12.x

before_install:
  - go get -u golang.org/x/lint/golint

script: make check

after_success:
  - bash <(curl -s https://codecov.io/bash)
